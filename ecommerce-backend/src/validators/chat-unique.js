

/**
 * Validates if the user already has a chat that is active
 */
const chatUniqueValidator = async (userId) => {
    // Get active chats for that user
    // If count is > 0, then return false
    // If count is <= 0 then return true
};

module.exports = {
    chatUniqueValidator
};